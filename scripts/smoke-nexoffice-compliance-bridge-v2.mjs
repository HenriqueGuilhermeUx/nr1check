import {createHmac,randomBytes} from 'node:crypto';

const {createNexOfficeFederatedSession}=await import('../apps/api/dist/integrations/nexofficeFederation.js');
const base=process.env.SMOKE_API_URL||'http://127.0.0.1:3000';
const secret=process.env.NEXOFFICE_COMPLIANCE_BRIDGE_SECRET;if(!secret)throw new Error('NEXOFFICE_COMPLIANCE_BRIDGE_SECRET required');
const workspaceRef=process.env.SMOKE_NEXOFFICE_WORKSPACE||'11111111-1111-4111-8111-111111111111';
const assert=(value,message)=>{if(!value)throw new Error(`ASSERT: ${message}`)};

function tokenFor(payload){const encoded=Buffer.from(JSON.stringify(payload)).toString('base64url');const sig=createHmac('sha256',secret).update(encoded).digest('base64url');return `${encoded}.${sig}`}
async function call(path,{method='GET',body,key,expect}={}){const headers={'content-type':'application/json'};if(key)headers['x-nexoffice-compliance-key']=key;const response=await fetch(base+path,{method,headers,body:body===undefined?undefined:JSON.stringify(body)});const payload=await response.json().catch(()=>({}));if(expect!==undefined){assert(response.status===expect,`${method} ${path}: expected ${expect}, got ${response.status} ${JSON.stringify(payload)}`);return{status:response.status,payload}}if(!response.ok)throw new Error(`${method} ${path} -> ${response.status} ${JSON.stringify(payload)}`);return payload}
function fakeClerk(existing=true){const calls={list:[],create:[],token:[]};const client={users:{async getUserList(params){calls.list.push(params);return{data:existing?[{id:'user_existing'}]:[]}},async createUser(params){calls.create.push(params);return{id:'user_created'}}},signInTokens:{async createSignInToken(params){calls.token.push(params);return{token:'ticket_single_use_test'}}}};return{client,calls}}

const handoffPayload={v:1,workspaceRef,businessName:'Empresa Bridge Teste',sector:'professional_services',exp:Math.floor(Date.now()/1000)+600,nonce:randomBytes(12).toString('base64url')};
const validToken=tokenFor(handoffPayload);
const verified=await call('/api/nexoffice/handoff/verify',{method:'POST',body:{token:validToken}});
assert(verified.source==='nexoffice','signed handoff source is NexOffice');assert(verified.workspaceRef===workspaceRef,'handoff is bound to workspace');assert(verified.businessName==='Empresa Bridge Teste','business context survives verification');assert(verified.privacy==='business_context_only','handoff exposes business context only');assert(!('email' in verified)&&!('cpf' in verified)&&!('employee' in verified),'handoff exposes no identity or employee data');
const tampered=validToken.slice(0,-1)+(validToken.endsWith('A')?'B':'A');await call('/api/nexoffice/handoff/verify',{method:'POST',body:{token:tampered},expect:401});const expired=tokenFor({...handoffPayload,exp:Math.floor(Date.now()/1000)-10,nonce:randomBytes(12).toString('base64url')});await call('/api/nexoffice/handoff/verify',{method:'POST',body:{token:expired},expect:401});

const federationInput={handoffToken:validToken,userRef:'nexoffice-user-123',userEmail:'OWNER@EXAMPLE.COM',userName:'Maria Empresária'};
const existing=fakeClerk(true);const first=await createNexOfficeFederatedSession(federationInput,existing.client,'https://nr1check.example.com');assert(first.accountProvisioned===false,'existing Clerk account is reused');assert(existing.calls.create.length===0,'existing auth account is never duplicated');assert(existing.calls.token[0]?.expiresInSeconds===120,'federated ticket is short-lived');let url=new URL(first.url);assert(url.origin==='https://nr1check.example.com'&&url.pathname==='/nexoffice/federated','federated redirect stays on source product');assert(JSON.stringify([...url.searchParams.keys()].sort())===JSON.stringify(['handoff','ticket']),'browser receives only ticket and opaque handoff');assert(url.searchParams.get('ticket')==='ticket_single_use_test'&&url.searchParams.get('handoff')===validToken,'opaque handoff is carried unchanged');assert(!url.searchParams.has('userEmail')&&!url.searchParams.has('userName')&&!url.searchParams.has('userRef')&&!url.searchParams.has('workspaceRef')&&!url.searchParams.has('businessName'),'identity and raw business context never appear in browser URL');assert(first.companyCreated===false,'federation never creates company or CNPJ record');
const fresh=fakeClerk(false);const second=await createNexOfficeFederatedSession(federationInput,fresh.client,'https://nr1check.example.com');assert(second.accountProvisioned===true&&fresh.calls.create.length===1,'missing auth account is provisioned once');assert(fresh.calls.create[0].emailAddress[0]==='owner@example.com'&&fresh.calls.create[0].skipPasswordRequirement===true,'passwordless account uses normalized server-side identity');assert(fresh.calls.create[0].publicMetadata.source==='nexoffice'&&fresh.calls.create[0].publicMetadata.nexofficeUserRef==='nexoffice-user-123','only safe auth provenance is stored');assert(fresh.calls.token[0].userId==='user_created','ticket is bound to provisioned auth account');

await call(`/api/internal/nexoffice/compliance-summary?workspaceRef=${workspaceRef}`,{key:'wrong-key',expect:401});const summary=await call(`/api/internal/nexoffice/compliance-summary?workspaceRef=${workspaceRef}`,{key:secret});assert(summary.workspaceRef===workspaceRef,'summary stays bound to linked workspace');assert(summary.sourceProduct==='nr1check','summary source is NR1Check');assert(summary.diagnosticStatus==='in_progress','active assessment is summarized without answers');assert(summary.programStatus==='attention','overdue action creates aggregate attention state');assert(summary.openActions===2,'open action count is aggregated');assert(summary.overdueActions===1,'overdue action count is aggregated');assert(summary.completionPct===33,'completion percentage derives from action status only');assert(summary.privacy?.mode==='aggregate_only','summary privacy contract is aggregate-only');const serialized=JSON.stringify(summary);for(const forbidden of ['52998224725','SECRET-PSYCHOSOCIAL-ANSWER','SECRET-COMPLAINT-CONTENT','SECRET-EVIDENCE','employeeName','psychosocialAnswers','complaintContent'])assert(!serialized.includes(forbidden),`summary must not leak ${forbidden}`);
const health=await call('/api/internal/nexoffice/health',{key:secret});assert(health.status==='ok'&&health.handoff==='signed'&&health.summary==='aggregate_only','bridge health declares safe contract');assert(health.rawEmployeeData===false&&health.rawPsychosocialAnswers===false&&health.complaintContent===false&&health.rawDocuments===false,'bridge health explicitly denies raw sensitive classes');
console.log(JSON.stringify({ok:true,workspaceRef,handoff:'signed',federation:{passwordless:true,ticketTtlSeconds:120,companyCreated:false,identityInBrowserUrl:false,businessContextOpaque:true},summary:{diagnosticStatus:summary.diagnosticStatus,programStatus:summary.programStatus,openActions:summary.openActions,overdueActions:summary.overdueActions,completionPct:summary.completionPct},privacy:summary.privacy},null,2));